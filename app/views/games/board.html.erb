<%= turbo_stream_from @game %>

<div id="<%= dom_id(@game, :board) %>" class="min-h-dvh bg-gray-950 text-white p-3 sm:p-4 md:p-6 lg:p-8 font-sans safe-px safe-pb"
     data-controller="refresh" data-refresh-interval-value="5">
  <div class="max-w-[1600px] mx-auto">

    <%= render "games/game_info", game: @game %>

    <!-- Список карточек игроков -->
    <div id="<%= dom_id(@game, :players_grid) %>" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 2xl:grid-cols-5 gap-4 md:gap-5 lg:gap-6">
      <% @players.each do |player| %>
        <%= render "players/host_card", player: player %>
      <% end %>
    </div>

  </div>
</div>
